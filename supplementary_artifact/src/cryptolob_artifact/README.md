# cryptolob_artifact

The executable public pipeline currently lives in `scripts/` and shares implementation through `scripts/artifact_lib.py`. This package stub is included so the supplementary artifact can evolve into an installable package without changing the reviewer-facing commands.

